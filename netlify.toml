[build]
  command = "next build"
  publish = "out"

# Sanity Studio is a client-side single-page app. Serve its HTML shell for any
# deep link under /studio so refreshes/direct links don't 404.
[[redirects]]
  from = "/studio/*"
  to = "/studio.html"
  status = 200
